<!-- HIVE:BEGIN -->
## Hive

This workspace is protected by Hive. Before editing files, read `.hive/agent-context.md`, run `node .hive/file-guard.mjs claim <path>` for each target file, and release it with `node .hive/file-guard.mjs release <path>` only when the file is safe for another agent.

<!-- HIVE:END -->
